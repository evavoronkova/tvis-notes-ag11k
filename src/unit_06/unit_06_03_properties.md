## Линейность математического ожидания

Пусть $X$ и $Y$ --- случайные величины, определённые на одном вероятностном пространстве,в том числе, возможно, зависимые. 

Тогда справедливы следующие утверждения:

1. $E(c\cdot X) = c\cdot EX$, где $c \in \mathbb{R}$;
2. $E(X+Y)=EX+EY$.

Докажем (1). По определению $E(c\cdot X)$ равно


  $$ E(c\cdot X) = \sum\limits_{i=1}^n c\cdot X(\omega_i)\cdot p_i = c\cdot \sum\limits_{i=1}^n \cdot X(\omega_i)\cdot p_i =c\cdot EX.$$

Теперь распишем $E(X + Y)$:

  $$E(X+Y) = \sum\limits_{i=1}^n ( X(\omega_i) +Y(\omega_i))\cdot p_i = \sum\limits_{i=1}^n \big( X(\omega_i)\cdot p_i +Y(\omega_i)\cdot p_i\big) = EX + EY.$$

Сформлируем общее утверждение:  
   
> Для произвольных случайных величин $X$ и $Y$, определённых на одном вероятностном пространстве, и произвольных вещественных чисел $a$ и $b$ выполнено равенство
> $$E(a\cdot X+b \cdot Y)=a\cdot EX + b \cdot EY.$$
> Это свойство называется **_линейностью_** математического ожидания.  



>[!NOTE]
Пиццерия приняла $100$ различных заказов  на доставку пиццы от студентов, но из-за сбоя в работе системы регистрации  все  заказы перепутались.  Каково математическое ожидание числа студентов, которые получат именно ту пиццу, которую они заказывали? Чему будет равно мат. ожидание, если число заказов было равно $1000$? 
>
> **Решение**
>
> - Пусть $X$ --- случайная величина, равная количеству правильных заказов. 
> - Вместо поиска распределения $X$ (что довольно сложно), представим
 ${X = X_1 + X_2 + \ldots + X_n}$, где $X_k$ --- бинарная случайная величина - индикатор события "$k$-ый клиент получил именно свой заказ", $n$ --- общее число заказов. 
> - $P(X_k = 1) = \cfrac{1}{n}, \;\; P(X_k = 0) = 1 - \cfrac{1}{n}$.
> - $EX = E(X_1 + \ldots + X_n) = EX_1 + \ldots EX_n$ (свойство линейности мат. ожидания); 
> -  $EX_k = P(X_k = 1) = \cfrac{1}{n}$ (свойство бинарной случайной величины);
> -  Тогда $EX = EX_1 +  \ldots + EX_n = n \cdot \cfrac{1}{n} = 1.$
> - Т.е. "в среднем" будет правильно выполнен $1$ заказ, ожидаемое число правильно выполненных заказов не зависит от общего числа заказов.


## Математическое ожидание произведения




> Пусть $X$ и $Y$ - случайные величины, определённые на одном вероятностном пространстве $\Omega$. 
> 
> Если величины $X$ и $Y$ ***независимы***, то
  $$E(X\cdot Y)=EX\cdot EY.$$

Докажем это утверждение.

- Пусть величина $X$ принимает значения $\{\alpha_1, \ldots, \alpha_k\}$, а величина $Y$ --- значения $\{\beta_1, \ldots, \beta_m\}$, т.е. $X: \Omega \to \{\alpha_1, \ldots, \alpha_k\},\;\; Y: \Omega \to \{\beta_1, \ldots, \beta_m\}$. 
- По определению   
  $$E(X\cdot Y) = \sum\limits_{i = 1}^{k} \sum\limits_{j = 1}^m y_i \cdot z_j \cdot P(X = y_i, Y = z_j)= $$
  
- так как $X$ и $Y$ --- независимы, то $P(X = \alpha, Y = \beta) = P(X = \alpha) \cdot P(Y = \beta)$, и можно записать (в продолжении предыдущего равенства)

  $$ =  \sum\limits_{i = 1}^{k} \sum\limits_{j = 1}^m y_i \cdot z_j \cdot P(X = y_i) \cdot P(Y = z_j)= $$
- соберём отдельно суммирование по $i$ и $j$: 
  $$ = \left(\sum\limits_{i = 1}^{k} y_i\cdot P(X = y_i) \right) \cdot \left(\sum\limits_{j = 1}^m  z_j   \cdot P(Y = z_j)\right) = EX\cdot EY.$$


**Замечание**  

Для случайных величин, которые _не являются независимыми_, утверждение может быть неверно.

